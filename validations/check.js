//const {check} = require('express-validator');

/* exports.validateRegisterUser = () => {
    return [ 
        check('user.tenkh', 'username does not Empty').not().isEmpty(),
        //check('user.tenkh', 'username more than 6 degits').isLength({ min: 6 }),
        check('user.email', 'Invalid does not Empty').not().isEmpty(),
        check('user.email', 'Invalid email').isEmail(),
        check('user.matkhau', 'password more than 6 degits').isLength({ min: 6 })
    ];
}; */
